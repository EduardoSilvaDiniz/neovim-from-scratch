return {
	ensure_installed = "all",
	sync_install = true,
	auto_install = true,
	TSUpdate = true,
	parser_install_dir = nil,
	ignore_install = {},
	highlight = {
		enable = true,
		additional_vim_regex_highlighting = true,
	},
	indent = { enable = true },
	modules = {},
}
