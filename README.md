script that install all dependencies for neovim (ubuntu only)
```bash
cd ~/.config
git clone git@github.com:EduardoSilvaDiniz/neovim-vanilla.git nvim
cd nvim && chmod +x install_base.sh && ./install_base.sh
```
